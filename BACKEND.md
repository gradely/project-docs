# Backend documentation

> The current, source-backed backend reference is [docs/backend-service-boundaries.md](docs/backend-service-boundaries.md).

The previous guide is preserved unchanged as [historical backend integration reference](docs/historical/backend-integration-reference.md). It includes useful background, but its fixed ports, deployment examples, authentication details, database schema examples, and external URLs must not be treated as verified current contracts.
